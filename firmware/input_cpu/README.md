# Input CPU

Arduino UNO R3 + 3.5" touch display firmware will live here.

Responsibilities:
- read touch/button user inputs
- send validated user intent to `Master CPU`
- never drive vehicle outputs directly
